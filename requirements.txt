# 必选依赖
# 基础依赖
# torch  # 注释掉以避免版本冲突，请手动确保torch>=2.0.0
numpy>=1.24.0  # 用于数值计算
pillow>=10.0.0  # 用于图像处理(PIL)
requests>=2.31.0  # 用于HTTP请求

# 音频处理依赖
scipy>=1.7.0  # 用于科学计算和音频处理
scikit-learn>=1.0.0  # 用于机器学习算法
# torchaudio  # 注释掉以避免版本冲突，请手动确保torchaudio兼容
librosa>=0.9.0  # 用于音频分析
soundfile>=0.10.0  # 用于音频文件读写

# 人声分离功能 (可选，需要兼容的torch版本)
# speechbrain>=0.5.0  # 快速语音分离 (可能与某些torch版本冲突，可手动安装)

# 可选依赖
python-dotenv>=1.0.0  # 用于环境变量管理(可选)
